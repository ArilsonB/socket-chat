pre-commit:
  parallel: true
  commands:
    test:
      run: pnpm test
commit-msg:
  scripts:
    "commitlint.sh":
      runner: bash
# commit-msg:
#   scripts:
#    "format-checker.sh":
#     runner: bash